'use strict';

var BaseModel = require('../../../common/base-model');

var instanceProps = {
  tableName: 'addresses_status'
};

var classProps = {};

module.exports = BaseModel.extend(instanceProps, classProps);
